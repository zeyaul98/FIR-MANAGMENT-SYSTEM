{
  "name": "railway-fir-management-system",
  "version": "1.0.0",
  "description": "Bihar Railway FIR Management System",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install && rm -rf dist && cp -r ../frontend/dist ./dist",
    "start": "cd backend && npm start || node server.js",
    "lint": "cd frontend && npm run lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": "18.x || 19.x || 20.x",
    "npm": "9.x || 10.x"
  }
}